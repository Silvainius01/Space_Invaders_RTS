#include "../ent/ent.h"
#include "../math.h"

//Knows how many units it has
//Knows what unit is doing wht, and how many are doing it
//Must be able to train units
//KNows howmany units the player has, but not what they are doing.
//Can prioritize units over buildings depending on if its iwnning or losing.